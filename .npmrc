# Temporary compatibility flag:
# @pbe/react-yandex-maps@1.2.5 still declares peer support only up to React 18,
# while the app now runs on React 19 and passes build/lint checks.
legacy-peer-deps=true
